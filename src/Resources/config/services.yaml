services:
    Bits\FlyBackendBundle\EventListener\AssetListener:
        arguments:
            $scopeMatcher: '@contao.routing.scope_matcher'
        tags:
            - { name: 'kernel.event_listener', event: 'kernel.request', priority: -2550 }
